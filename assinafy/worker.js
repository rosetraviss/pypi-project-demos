// Placeholder file required to satisfy specific requirements.
// Main execution flow is managed by src/index.py
export default {
  async fetch(request, env, ctx) {
    return new Response("Not configured for JS worker.");
  }
};
