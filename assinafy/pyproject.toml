[project]
name = "assinafy-demo"
version = "0.1.0"
description = "assinafy SDK demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "assinafy>=1.3.1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
