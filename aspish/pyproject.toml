[project]
name = "aspish-demo"
version = "0.1.0"
description = "aspish PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "aspish",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
