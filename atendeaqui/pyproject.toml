[project]
name = "atendeaqui-demo"
version = "0.1.0"
description = "AtendeAqui PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "atendeaqui>=1.0.1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
