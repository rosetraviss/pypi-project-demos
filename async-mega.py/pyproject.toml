[project]
name = "async-mega-py-demo"
version = "0.1.0"
description = "async-mega.py demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "async-mega-py",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
