[project]
name = "currency-converter-demo"
version = "0.1.0"
description = "CurrencyConverter PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "CurrencyConverter>=0.18",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
