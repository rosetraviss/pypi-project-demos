[project]
name = "asn1-demo"
version = "0.1.0"
description = "asn1 PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "asn1",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
