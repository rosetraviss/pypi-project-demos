[project]
name = "async-requests-tls-demo"
version = "0.1.0"
description = "Demo for async-requests-tls"
requires-python = ">=3.13"
dependencies = [
    "async-requests-tls==0.1.0",
]

[tool.uv]
package = false
