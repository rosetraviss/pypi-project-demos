[project]
name = "atlas-smilies-demo"
version = "0.1.0"
description = "atlas-smilies PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "atlas-smilies>=0.1.0",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
