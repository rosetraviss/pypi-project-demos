[project]
name = "audiosentinel-demo"
version = "0.1.0"
description = "Demo for audiosentinel"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["audiosentinel"]

[tool.uv]
package = false
