[project]
name = "ass-imbk-demo"
version = "0.1.0"
description = "ass-imbk PyPI demo running as a Cloudflare Python Worker"
requires-python = ">=3.12"
dependencies = [
    "ass-imbk>=1.0.4",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
