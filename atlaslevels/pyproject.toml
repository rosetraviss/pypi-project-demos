[project]
name = "atlaslevels-demo"
version = "0.1.0"
description = "Cloudflare Worker demo for atlaslevels"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "atlaslevels==0.3.0",
]

[tool.uv]
package = false
